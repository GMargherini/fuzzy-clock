# Fuzzy clock
Tells the local time with a precision of ±2.5 minutes

## Build
 ```cargo build [--release]```

## Usage
```./fuzzy_clock [language] [t]```

### Available languages
- English -> en
- French -> fr
- Italian -> it
- Swedish -> sv

